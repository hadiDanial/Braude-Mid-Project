import java.util.HashSet;
import java.util.LinkedList;
import java.util.Map.Entry;
import java.util.Set;

/**
 * search for shortest paths in the graph
 **/ 
 public class Dijkstra {
 
	
	/**
	 * calculates shortest path from the source node to all nodes of the graph 
	 * @param graph  the graph to be analyzed
	 * @param source  the source node
	 * @return the graph structure containing in the field shortestPath the shortest path from source to the given node 
	 * and in the field distance the length of the shortest path
	 */
	public static Graph calculateShortestPathFromSource(Graph graph, Node source) {
	//not implemented yet  
    return null;
	}
	
	/**
	 * calculates the length of the shortest path from the source node to the evaluation node in the graph; 
	 * the result is saved in the field shortestPath of evaluation node (the shortest path from source to the given node) 
	 * and in the field distance (the length of the shortest path)
	 */
	private static void calculateMinimumDistance(Graph graph, Node evaluationNode, Node sourceNode) {
	//not implemented yet     
	}
}
